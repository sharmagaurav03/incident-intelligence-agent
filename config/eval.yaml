# mock everything -- runs with no credentials
data_dir: data_eval
output_dir: out_eval
source_dir: sample_service
llm:
  mode: mock
adapters:
  tickets: file
  logs: file
  metrics: file
  fix_publisher: file
